### Step 1.3 — Authorization layer (server)

* Funzione/utility `authorize(action, resourceScope)`
* Guard su API/Server Actions + UI gating (hide/disable)

  **DoD:** nessuna azione sensibile passa senza check; test minimo su 5 azioni.

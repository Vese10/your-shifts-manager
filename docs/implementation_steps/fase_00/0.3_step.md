### Step 0.3 — Auth (Clerk) + Protezione route

- Clerk setup (login/signup/invite)
- Middleware route protection
- Session user in server actions/route handlers

  **DoD:** login funzionante, redirect per non-auth, user visibile in app.
